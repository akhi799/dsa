void insertionSort2(int n, vector<int> arr) {
    for(int i=1;i<n;i++){
        int j=i;
        int key= arr[i];
        while(j>=0&&key<=arr[j]){
            if(key<arr[j])arr[j+1]=arr[j];
            j--;
        }arr[j+1]=key;
        printarray(arr);
    }

}
